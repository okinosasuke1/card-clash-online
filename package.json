{
  "name": "jforce-ghn-shopify-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Create GHN shipments from paid Shopify orders and update Shopify tracking.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check scripts/register-webhook.js",
    "register-webhook": "node scripts/register-webhook.js"
  },
  "engines": {
    "node": ">=20"
  }
}

